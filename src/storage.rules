rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all files so they can be displayed in the chat.
    match /{allPaths=**} {
      allow read;
    }
  }
}